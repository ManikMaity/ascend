{
  "name": "ascend",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --ui tui",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:component": "turbo run test:component",
    "db:generate": "turbo run db:generate --filter=@ascend/db",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "bun --filter @ascend/db db:migrate",
    "verify:server": "bun scripts/verify-server.mjs",
    "verify:expo": "bun scripts/verify-expo.mjs"
  },
  "devDependencies": {
    "@types/react": "~19.2.4",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "bun@1.2.18",
  "overrides": {
    "@types/react": "~19.2.4",
    "better-auth": "1.7.1",
    "@better-auth/expo": "1.7.1"
  }
}
